<form method="post" action="{{ path('app_horaire_delete', {'id': horaire.id}) }}" onsubmit="return confirm('Confirmez-vous la suppression de cet item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ horaire.id) }}">
    <button class="btn">Supprimer</button>
</form>
