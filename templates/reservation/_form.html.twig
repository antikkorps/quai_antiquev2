
<div class="mt-5">
{{ form_start(form) }}
{{ form_widget(form.date)}}
{{ form_widget(form.allergies) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
</div>